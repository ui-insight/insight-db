# Superuser password for the shared postgres instance
POSTGRES_PASSWORD=change_me_to_a_secure_password

# Per-app passwords (update init/01-create-databases.sql to match)
# These are set in each app's own .env file, not here.
# Listed for reference only:
#   OpenERA:            OPENERA_DB_PASSWORD
#   UCM Daily Register: UCM_DB_PASSWORD
#   Audit Dashboard:    AUDIT_DB_PASSWORD
